/*
  X-Robots-Tag: noindex, nofollow, noarchive, noimageindex

# Keep Pages' default ETag revalidation for /assets/*. A new deployment can
# briefly return the SPA fallback before an asset reaches every edge, and an
# immutable override would pin that HTML response to the asset URL.
