/*
  X-Robots-Tag: noindex, nofollow, noarchive, noimageindex

# Keep Pages' status-specific cache headers. /assets/404.html makes asset
# misses real 404 responses while preserving the root SPA fallback.
